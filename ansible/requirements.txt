# Local controller Python dependencies for direct ansible-playbook runs.
# Needed for VMware tasks delegated to localhost.
ansible-core==2.18.17
requests==2.33.0
pyvmomi==9.1.0.0
# Needed by community.hashi_vault modules delegated to localhost.
hvac==2.4.0
