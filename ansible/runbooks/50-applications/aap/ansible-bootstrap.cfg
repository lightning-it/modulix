[defaults]
# The local Podman bootstrap intentionally runs before installation secrets
# exist. Keep it independent from the project-wide Vault configuration.
retry_files_enabled = False
interpreter_python = auto_silent
