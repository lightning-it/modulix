# Platform Runbooks

Platform runbooks configure host or cluster substrates that applications can run
on.

Use this area for:

- Incus hosts
- Incus image artifact staging and imports
- OpenShift clusters and supporting hosts
- future platform substrates

Application-specific deployment belongs in `50-applications/`.
