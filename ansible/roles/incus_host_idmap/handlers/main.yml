---
- name: Restart Incus after subordinate ID changes
  ansible.builtin.systemd_service:
    name: "{{ incus_host_idmap_service }}"
    state: restarted
  listen: Restart Incus after subordinate ID changes
...
